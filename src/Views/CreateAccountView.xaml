<UserControl x:Class="AsyncToolWindowSample.Views.CreateAccountView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AsyncToolWindowSample.Views"
             xmlns:toolWindows="clr-namespace:AsyncToolWindowSample.ToolWindows;assembly=Grader"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" d:DataContext="{d:DesignInstance toolWindows:CreateAccountViewModel}">
    <Grid Margin="10">
        <StackPanel>
            <Label Content="Name"></Label>
            <TextBox Text="{Binding Person.Name}"></TextBox>
            <Button Command="{Binding CreateAccountCommand}" Content="Create Account"/>
            <Button Content="Back" Command="{Binding BackCommand}"></Button>
        </StackPanel>
    </Grid>
</UserControl>
